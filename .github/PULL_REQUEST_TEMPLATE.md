## What changes

<!-- One or two sentences. What behaviour is different after this? -->

## Why

<!-- The problem this solves. Link an issue if there is one. -->

## How it was checked

- [ ] `npm run test:unit` is green
- [ ] `npm run package` succeeds
- [ ] Any behaviour change leaves a runnable check behind
- [ ] Live sweeps run if the change touches the card, the options page or the popup

## House rules

- [ ] No comments in extension source
- [ ] No em-dash or en-dash in text the user sees
- [ ] Commit messages start with `added:`, `fixed:`, `changed:` or `removed:`
